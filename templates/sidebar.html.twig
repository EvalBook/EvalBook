<div class="container-fluid">

    <div class="row" id="container">
        <div class="col-sm-4 dropdown-menu" id="navbar">
            <div class="dropdown-item">{{ "Notebook" | trans }}</div>
            <div class="dropdown-item">{{ "Activities" | trans }}</div>
            <div class="dropdown-item">{{ "School report" | trans }}</div>
            <div class="dropdown-item">{{ "Pupils" | trans }}</div>
            <div class="dropdown-item">{{ "Classes" | trans }}</div>
            <div class="dropdown-item">{{ "Schools and implantation" | trans }}</div>
            {% if is_granted('ROLE_USER_DELETE') %}
            <a href="{{ path('users_index') }}" class="dropdown-item">{{ "Users" | trans }}
                {% endif %}
            </a>
        </div>
    </div>

</div>
