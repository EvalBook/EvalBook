{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>ActiveInPeriod</th>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ activite.comment }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activite.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activites') }}">back to list</a>

    <a href="{{ path('activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('activite/_delete_form.html.twig') }}
{% endblock %}
