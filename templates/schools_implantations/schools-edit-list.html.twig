{% extends 'base.html.twig' %}
{% trans_default_domain 'templates' %}

{% block title %}{{ 'schools-implantations.title.edit-list-schools' | trans }}{% endblock %}

{% block sidebar %}
    {% include '_partials/sidebar.html.twig' %}
{% endblock %}

{% block body %}
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">{{ 'schools-implantations.th.id' | trans }}</th>
            <th scope="col">{{ 'schools-implantations.th.name-school' | trans }}</th>
            <th scope="col"></th>
        </tr>
        </thead>
        <tbody>
        {% for school in schools %}
            <tr>
                <th scope="row">{{ school.id }}</th>
                <td>{{ school.name }}</td>
                <td>
                    <i style="cursor: pointer;" class="far fa-edit" data-toggle="modal" data-target="#modal-right-school-{{ school.id }}"></i>
                    {% include 'schools_implantations/school-edit-form.html.twig' with {'form' : schoolsForms[school.id], 'id': school.id} %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">{{ 'schools-implantations.not-found-school' | trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}