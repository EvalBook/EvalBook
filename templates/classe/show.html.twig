{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classes') }}">back to list</a>

    <a href="{{ path('classe_edit', {'id': classe.id}) }}">edit</a>

{% endblock %}
