{% extends 'base.html.twig' %}
{% trans_default_domain 'templates' %}

{% block pageTitle %} {{ 'Activities' | trans }} {% endblock %}
{% block title %}{{ 'Select implantation / class pairs' | trans }}{% endblock %}

{% block body %}
    <table class="full-width">
        <thead>
        <tr>
            <th>{{ 'Implantation'|trans }}</th>
            <th>{{ 'Class'|trans }}</th>
            <th>{{ 'Owner'|trans }}</th>
            <th class="text-center mobile-hidden-td">{{ 'Activities'|trans }}</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for classe in classes %}
            <tr>
                <td>{{ classe.implantation }}</td>
                <td>{{ classe.name }}</td>
                <td>
                    {%  if app.user is same as(classe.titulaire) %}
                        <span><< <strong>{{ 'Me'|trans }}</strong> >></span>
                    {% else %}
                        {{ classe.titulaire }}
                    {% endif %}
                </td>
                <td class="text-center mobile-hidden-td">{{ classe.activites|length }}</td>
                <td>
                    <a href="{{ path('activite_add', {'classe': classe.id} ) }}" title="Add activity to the class">
                        <i class="fas fa-plus text-yellow"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" class="text-center">
                    {{ 'You do not have any class'|trans }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}